// With this we can handle react states in more professional way and with this we can understand the  structure of Context API and state management tool like Redux.
// What is use Reducer -- it is react hook that helps to manage the state of react components, it's just alternative to useState, they both create new state and updated it -- the difference is the way to update state

function App() {
  return (
    <></>
  );
}

export default App;
