import React from 'react'
// use case 1 -- when you update multiple state in single function 
// so when 2 or more state together it's better to use UseReducer hook
// use case 2 -- if we have a complex state with different type of object we should use the useReducer
const WhenTOUseReducerVsUseState = () => {
  return (
    <div>WhenTOUseReducerVsUseState</div>
  )
}

export default WhenTOUseReducerVsUseState