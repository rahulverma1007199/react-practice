// This is continuation of previous object

// So for dark mode we have created a new component and we paste two icons -- 🌕🌙

// and use this componet in navbar 
// for that we use context api

// when use context api -- component should be client side -- ThemeContext.js i mean.

// we take children props in case where the file in wrapper -- like ThemeContext.js , Layout etc.

// we wrap the ThemeContext in main layout file

// we use className =mode in context and use class name in global.css and take bgc and color property from body

// we give theme class name for animation effect